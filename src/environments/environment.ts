// Produccion (default que usa `ng build`).
// IMPORTANTE: cuando Render te asigne la URL, reemplaza el valor de abajo.
export const environment = {
  production: true,
  apiUrl: 'https://hotel-praia-api.onrender.com'
};
